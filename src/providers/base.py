"""Abstract provider interface. Every provider adapter implements this contract
so the harness can swap providers with a one-line config change.
TODO (Week 2): define the common interface (e.g. .call(messages, tools) -> response)
"""
